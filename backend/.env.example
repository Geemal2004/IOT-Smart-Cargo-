# ── MongoDB ────────────────────────────────────────────────
MONGO_URI=mongodb://127.0.0.1:27017/cargo_monitor

# ── MQTT Broker ────────────────────────────────────────────
# Use mqtts:// for TLS (port 8883)  |  mqtt:// for plain (port 1883)
MQTT_BROKER_URL=08d68a0332ec419da59fbcf26b106e44.s1.eu.hivemq.cloud:8883
MQTT_USERNAME=Management
MQTT_PASSWORD=Management123
# Path to broker's CA cert file (PEM). Leave blank to use system CAs.
MQTT_CA_CERT_PATH=./certs/ca.crt

# ── Express / Socket.IO ────────────────────────────────────
PORT=4000

# ── Alert Thresholds ───────────────────────────────────────
ALERT_TEMP_MAX=8.0
ALERT_SHOCK_G_MAX=2.5

# ── CORS Origin (frontend dev server) ─────────────────────
CORS_ORIGIN=http://localhost:3000
